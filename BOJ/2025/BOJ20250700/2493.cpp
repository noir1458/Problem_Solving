#include<iostream>
#include<bits/stdc++.h>
using namespace std;
int main(){
    cin.tie(0);cout.tie(0);
    ios::sync_with_stdio(0);

    int T;
    cin >> T;
    vector<int> arr;
    while(T--){
        int tmp;
        cin >>tmp;
        arr.push_back(tmp);
    }

    for(auto e:arr){
        cout <<e;
    }

    vector<int> res;
    while(T--){
        res.push_back(0);
    }
    
    int num = arr.size()-1; // 맨 끝 번호부터
    while(ar)

    


    return 0;
}